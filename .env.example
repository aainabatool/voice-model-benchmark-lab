APP_ENV=development
LOG_LEVEL=INFO

# Full Postgres for staging/prod; SQLite is fine for local dev (see settings.py default)
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/voice_benchmark

ARTIFACTS_DIR=./artifacts
DATASETS_DIR=./datasets

DEFAULT_DEVICE=auto
